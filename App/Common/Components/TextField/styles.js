import { StyleSheet } from 'react-native';
import { MainPalette } from '../../Constants/colors';

export default StyleSheet.create({
  inputMessageWrapper: {
    paddingLeft: 16,
  },
  errorMsgColor: {
    color: MainPalette.burntSienna,
  },
  btnClearIcon: {
    color: MainPalette.mineShaft,
  },
});
